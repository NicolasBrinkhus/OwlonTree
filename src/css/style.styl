// Import all partials
@require './_*.styl'

.content{

}

.header {
  background-image: linear-gradient(rgba(16, 29, 44, 0.80), 
    rgba(16, 29, 44, 0.80)), url('./images/Eleg-1.fw.png');
  background-size: cover;
  background-position: center;
  padding: 16rem;
}

header.top
  text-align center
  .owl
    color darkgreen
    margin-top 3rem
  .tree
    color darkgreen
    margin-top 3rem
    margin-left -4rem
  h1
    font-size 10rem
    line-height 1.8 // this font has a wacky baseline
    display flex
    justify-content: center;

    // This is a bunch of goofy CSS to make the logo look decent
    .ofThe
      display flex
      font-size 4rem
      color orange
      justify-content: center;
      align-items: center;
      background url('images/OwlintheTree.fw.png') center no-repeat;
      background-size cover
      padding 7rem 5rem 7rem 6rem
    .of
      padding-right 2rem
      position relative
      right -1.5rem
      top 1rem
    .the
      padding-right 2rem
      position relative
      right -2.5rem
      top 1rem
  h3
    margin 0
    margin-left 3rem
    font-size 2rem
    color orange
    position relative
    display inline-block
    span
      background white
      position relative
      z-index 2
      padding-left 1rem
      padding-right 1rem
    &:before, &:after
      display block
      z-index 1
      background black
      position absolute
      width 130%
      height 1px
      content ''
      top 5px
      margin-left -15%
    &:after
      top auto
      bottom 7px


.owl-in-the-tree 

  display flex
  height 90vh
  max-width 1500px
  margin 0 auto
  margin-top 5vh

  @media only screen and (max-width: 600px){
    flex-direction: column-reverse;
  }

  & > * 

    flex 1 4 auto
    padding 2rem
    border 1rem double lighten(black,10%)
    position relative
    background white
    transition all 0.3s
    box-shadow 0 5px 5px rgba(0,0,0,0.1)
    overflow scroll
    &:first-child
      flex-shrink 1 // take 4x the extra room
      flex-basis 65%
    &:nth-child(2)
      border-left 0
      border-right 0
      min-width 300px
    &:last-child
      flex-shrink 1 // take 4x the extra room
      flex-basis 35%
  


// Folding Transforms
// Take off folding when not checked
input#fold:not(:checked) ~ #main
  .owl-in-the-tree > *
      transform none

label[for="fold"]
  position absolute
  top 1rem
  left 1rem
  text-transform uppercase
  font-size 1.3rem
  background black
  color white
  border 2px solid black
  cursor pointer
  padding 0.5rem 1rem

input#fold
  display none
  &:checked + label
    background white
    color black

ul
  list-style none
  margin 0
  padding 0

ul.order
  // Default state
  li
    border-bottom 1px solid black
    padding 2rem 0
    display flex
    font-size 1.4rem
    justify-content space-between
    align-items center
    &:hover
      // padding 1rem 0
      button
        display inline
    button
      border 0
      display none
      line-height 1
      padding 0
    &.total
      font-weight 600
      border-bottom 3px solid black
      border-top 3px double black
    &.unavailable
      text-decoration line-through
      background lighten(red, 80%)
    .price
      font-size 1.2rem
    span.count
      position relative
      overflow hidden
      float left // only works if it's floated?!
      span
        display inline-block
        // transition all 0.5s

.order-wrap {
  text-align: center;
  line-height: 1.6;
}

.order-title
  text-align center

.btn--checkout {
  padding: .5rem 1rem;
  transition: all .3s;
  transform: scale(1);
}
.btn--checkout:disabled{
  transform: scale(.2);
  display: none;
}

.btn--logout {
  position: absolute;
  top: 3%;
  right: 3%;
}

.meal-edit
  margin-bottom 20px
  border 2px solid black
  overflow hidden
  display flex
  flex-wrap wrap
  input, textarea, select
    width 33.33%
    padding 10px
    line-height 1
    font-size 1.2rem
    border 0
    border-bottom 1px solid black
    border-right 1px solid black
    appearance none
    border-radius 0
    background white
    &:focus
      outline 0
      background lighten(orange, 85%)
  textarea
    width 100%
  input:last-of-type
    width 100%
  button
    width 100%
    border 0


// Menu Styles
.list-of-fish
  border-top 2px solid black
  border-bottom 1px solid black
  padding-top 5px
  margin-top 2rem
  transform translateZ(0);

.menu-meal
  border-bottom 2px solid black
  border-top 1px solid black
  padding-bottom 2rem
  padding-top 2rem
  margin-bottom 5px
  clear both
  overflow hidden
  p
    margin 0
    font-size 1.8rem
  .meal-name
    margin 0
    display flex
    justify-content space-between
    align-items center
  .price
    font-size 1.4rem
    // color orange
    justify-content flex-end
    // font-family 'Open Sans Condensed'
  img {
    float left
    width 150px
    margin-right 1rem
    @media only screen and (max-width: 730px){
      width: 100px;
    }
  }

button, input[type=submit]
  text-transform uppercase
  background none
  border 1px solid black
  font-weight 600
  font-size 1.5rem
  font-family 'Open Sans'
  transition all 0.2s
  position relative
  z-index 2
  &[disabled]
    color red
    background white
    border-color red
    transform rotate(-10deg) scale(2) translateX(50%) translateY(-50%)
    &:hover
      color red
      cursor not-allowed
    &:after
      display none
  &:after
    content ''
    z-index -1
    display block
    background black
    position absolute
    width 100%
    height 0
    left 0
    top 0
    transition all 0.2s
  &:hover, &:focus
    color white
    outline 0
    &:after
      height 100%
  // variants
  &.warning
    &:after
      background red
  &.success
    &:after
      background green

  &.github, &.facebook, &.twitter
    border 0
    display block
    margin-bottom 2rem
    width 100%
    color white
    padding 2rem

  &.github
    background #82D465
    &:after
      background darken(#82D465, 20%)

  &.facebook
    background #3864A3
    border-radius: 3px
    overflow: hidden
    &:after
      background darken(#3864A3, 20%)

  &.twitter
    background #5EA9DD
    &:after
      background darken(#5EA9DD, 20%)

.test-btn{
  width: 100%
  padding: 1rem 2rem
  background-color yellow
  border-radius: 3px;
  border: none
  overflow: hidden
  &:after{
    background-color darken(#ff0, 50%) !important
  }
}

.register_form {
  display: flex;
  flex-direction: column;
  padding: 1rem;
  border-radius: 5px;
  background-color: #3ac53a;

  &-name {
    padding: 1rem 1rem;
    background-color: #eee;
    color: black;
    border: none;
    outline: none;
    border-bottom: 1px solid #ccc;
  }
  &-email {
    padding: 1rem 1rem;
    background-color: #eee;
    color: black;
    border: none;
    outline: none;
    border-bottom: 1px solid #ccc;
  }
  &-tel {
    padding: 1rem 1rem;
    background-color: #eee;
    color: black;
    border: none;
    outline: none;
    border-bottom: 1px solid #ccc;
  }
  & button {
    border-radius: 5px;
    margin-top: 2rem;
  }
}


// Store Selector
.store-selector
  background transparent
  max-width 500px
  margin auto
  margin-bottom 10.5rem
  color #c69963
  padding 2rem
  text-align center
  border 2px solid #c69963
  text-shadow .1rem .1rem rgba(black, .8)
  input
    width 100%
    padding 1.5rem
    color #c69963
    text-shadow .1rem .1rem rgba(black, .8)
    background-color transparent
    border: none;
    &[type="text"]
      text-align center
      font-size 3rem
  button
    padding 1.5rem 
    color #030329
    background-color #c69963
    border: none
    &:hover{
      color #c69963
    }
.below
  position: relative
  h2 
    color: #c69963
    text-shadow .1rem .1rem rgba(black, .8)
  
.arrow
  position: absolute
  top: 50%
  left: 48%
  transform: translate(-50%, -50%)
  font-size: 15rem
  text-shadow .1rem .1rem rgba(black, .8)
  color: #c69963

.bounce {
  -moz-animation: bounce 2s infinite;
  -webkit-animation: bounce 2s infinite;
  animation: bounce 2s infinite;
}    

// ABOUT CONTENT && TEXT \f25a
.text_content
  width 100%
  padding 10rem

  &-about
    font-size 3rem
    margin-top 4rem
    text-align center
    
    h2:after
      content: "";
      display: block;
      height: 2px;
      width: 150px;
      background: #f1933c;
      margin: 30px auto;
    
    p
      width 100%
      padding 0 10rem
      font-size 1.8rem
      font-weight: 300
      line-height: 1.8

  &-info
    display flex
    width: 100%
    align-items: center
    margin-top 10rem
    margin-bottom 4rem
    overflow: hidden

    &--txt
      width: 50%
      color: #4c4848
      line-height: 1.8
      text-align: center
      margin-left 2rem
      padding 10rem 5rem 5rem 5rem
      border 2px solid #ccc
      border-right none
      background-color #f7f7f7

      h2:after
        content: "";
        display: block;
        height: 2px;
        width: 150px;
        background: #f1933c;
        margin: 30px auto;

      p
        font-size: 1.7rem
        font-weight: 300
        justify-content: center
      
    &--img
      width: 50%
      transform: translateX(30%) scale(0.9);
      opacity: 0;
      transition: all .3s;
      
      &.active
        transform: translateX(0) scale(1) !important;
        opacity: 1 !important;

      img
        width: 100%

        

// POPUP SESSION
.popup {
  height: 100vh;
  width: 100%;
  position: fixed;
  top: 0;
  left: 0;
  background-color: rgba(#000, .8);
  z-index: 9999;
  opacity: 0;
  visibility: hidden;
  transition: all .3s;

  &:target {
    opacity: 1;
    visibility: visible;
  }

  &__content {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 65%;
    height: 95vh;
    background-color: white;
    box-shadow: 0 2rem 4rem rgba(#000, .2);
    border-radius: 3px;
    opacity: 0;
    transform: translate(-50%, -50%) scale(.2);
    transition: all .5s;

    &--header {
      margin-top: 3rem;
    }
  }

  &:target &__content {
    opacity: 1;
    transform: translate(-50%, -50%) scale(1);
  }
}
 // google maps
.deliver_content
  display: flex
  width: 100%
  flex-direction: column
  align-items: center
  
  &-title
    margin-top 4rem

  &-map
    width: 80%
    height: 80vh
    padding: 1rem
    background-color grey
    
// ADDRESS CONFIRMATION

.popup__content-address {
  display: flex;
  flex-direction: column
  align-items: center
  width: 100%
  padding: 2rem
  
  & h2{
    margin-bottom 2rem
  }
  
  &--form {
    width: 80%
    height: 55rem
    display: flex
    flex-direction: column
    justify-content: space-evenly
    & textarea {
      outline: none
      border 1px solid darkgreen
    }
  }
  
  &--inputs{
    width: 100%
    display: flex
    align-items: center

    & input{
      outline: none
      width: 98%
      height: 4rem
      padding: 1rem
      border: 1px solid red
      &:required:valid{
        border-color darkgreen
      }
      &:required:invalid + span{
        opacity: 0
      }
    }
    
    & p {
      color: darkblue;
      text-transform: uppercase;
      padding: 1rem;
    }

    & span{
      width: 2%
      height: 4rem
      color: darkgreen
      opacity: 1
      transition: opacity .2s;
    }
  }
}